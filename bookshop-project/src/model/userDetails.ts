/** Here we define the data type of userDetails' properties like username, password, role and purchases. */
export interface UserDetails {
    username: string;
    password: string;
    role: string;
    purchases: string;
  }
  